const form = document.getElementById("form");
const button = document.getElementById("submitBtn");

form.addEventListener('submit', function submitQuiz() {
    const name = document.querySelector('#name').value
    const hair = document.querySelector('input[name=hair]:checked').value
    const hairtone = document.querySelector('input[name=hairtone]:checked').value
    const tan = document.querySelector('input[name=tan]:checked').value
    const palms = document.querySelector('input[name=palms]:checked').value
    const eyes = document.querySelector('input[name=eyes]:checked').value
    const jewelry = document.querySelector('input[name=jewelry]:checked').value
    const flower = document.querySelector('input[name=flower]:checked').value
    const sunburn = document.querySelector('input[name=sunburn]:checked').value
    const veins = document.querySelector('input[name=veins]:checked').value
    const gemstone = document.querySelector('input[name=gemstone]:checked').value
    const eyelashes = document.querySelector('input[name=eyelashes]:checked').value
    const lips = document.querySelector('input[name=inside_lip]:checked').value
    const cheeks = document.querySelector('input[name=cheeks]:checked').value
    const skintone = document.querySelector('input[name=skintone]:checked').value
    
    
    })
    button.addEventListener("click", form);
    



    /* const season = "winter";

    switch (season) {
        case ""
    }
 */
    

   
    

